# SpeechControlledCommandLineSystem

It is a speech controlled command line system to play around with. Install `portaudio` in your system and `SpeechRecognition`, `colorama` module of python to run this project.
